SUPERSEDED — August 12, 2026
=============================

Stale, describes the old PDF/BookFlip build. Ignore below this line.

For the current build, check on a real iPhone once deployed:
  1. Home page loads and shows both covers within ~1-2s on cellular.
  2. Tap "Open reader" — page 1 appears quickly (~130 KB image).
  3. Swipe left/right through several pages; no blank flashes.
  4. Double-tap a page to zoom in, double-tap again to reset; pinch-zoom
     also works.
  5. Tap the search icon, type a word that appears in the newsletter,
     tap a result — jumps to that page.
  6. Tap the small "PDF" link on the home page — opens the PDF in a new
     tab (Safari's own viewer), does not silently fail.
  7. Rotate the phone; layout should resize with no missing page.
